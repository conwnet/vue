import assert from 'assert';
import { indexOf } from '~/util/lang';

/*
describe('util/lang.js', () => {
    it('indexOf', () => {
        let arr = [1, 2, 3, 4, 5];
        assert.equal(indexOf(arr, 3), 2);
    })
});
*/